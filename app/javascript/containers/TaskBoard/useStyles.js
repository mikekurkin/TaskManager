import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    height: '100%',
  },
  addButton: {
    position: 'fixed',
    bottom: 32,
    right: 32,
  },
}));

export default useStyles;
