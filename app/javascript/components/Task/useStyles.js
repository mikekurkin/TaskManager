import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: 250,
    marginLeft: 8,
    marginRight: 8,
    marginBottom: 4,
  },
}));

export default useStyles;
